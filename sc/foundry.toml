[profile.default]
src = "src"
out = "out"
libs = ["lib"]

# Compilador
solc_version = "0.8.19"
evm_version = "paris"
optimizer = { enabled = true, runs = 200 }

# Testing
[profile.test]
optimizer = { enabled = true, runs = 200 }
